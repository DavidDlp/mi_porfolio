import React from 'react';
import AboutMe from '../pages/AboutMe';

const Home = (title, dark) =>{

    return (
        <div>
            <AboutMe dark={true}/>
        </div>
    )
};

export default Home